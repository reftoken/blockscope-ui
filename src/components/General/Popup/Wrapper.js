import styled from 'styled-components';

const Wrapper = styled.div`
  background: white;
  margin-top: 10vh;
  max-height: 80vh;
  padding: 25px 50px;
  overflow-y: auto;
  height: auto;
  position: relative;
`;

export default Wrapper;
